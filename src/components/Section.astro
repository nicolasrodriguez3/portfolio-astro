---
interface Props {
	title: string
	class?: string
}

const { title, class: className } = Astro.props
---

<section class=`py-20 ${className}`>
	<h2 class="text-2xl font-semibold text-blue-300/90">{title}</h2>
	<slot />
</section>
